<h1>DBC Vantage Point</h1>

<form class="create_event" action="/create" method="POST">
  <input type="text" name="event[name]" placeholder="Create Event Name">
  <input type="text" name="event[tag]" placeholder="Create Hashtag #">
  <input type="submit">
</form>

<div class="instructions">
  <h3>How to</h3>
  <ol>
    <li>Name the event</li>
    <li>Pick a hashtag (leave out #)</li>
    <li>Tag with #DBCvantagepoint and your hashtag</li>
    <li>Party</li>
  </ol>

</div>

<ul class="event_list"> 
  <% @all_events.each do |event| %> 
    <div class='event_format'>
      <div class="navi" id="photo_list"><a href="/event/<%= event.id %>">#<%= event.tag %></a></div>
      <% if event.images.first %>
      <a href="/event/<%= event.id %>"><img class="event_img" src="<%= event.images.last.url%>"></a>
      <% else %>
      <a href="/event/<%= event.id %>"><img class="event_img" src="images/placeholder_directions.png"></a>
      <% end %>
    </div>
  <% end %>
</ul>

<!-- <div class="container">
  <h1>Welcome to VantagePoint!</h1>

  <form action="/create" method="POST" class="event_input">
      <label>Event Name: </label>
      <input type="text" name="event[name]">
      <label>Hashtag (no #): </label>
      <input type="text" name="event[tag]">
      <input type="submit">
  </form>

  <ul class="events"> 
    <% @all_events.each do |event| %> 
    <li> <a href="/event/<%= event.id %>"><%= event.name %></a></li>
    <% end %>
  </ul>

</div>
 -->
